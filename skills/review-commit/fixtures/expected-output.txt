## Critical Issues (Must Fix) [confidence 95-100]
Bugs, security risks, confirmed PII leaks, correctness failures.
- `internal/auth/login.go:22` — SQL injection: user input concatenated into query — use parameterized query `db.Query("SELECT id FROM users WHERE name=? AND pw=?", user, pass)`. [confidence: 98]
- `internal/auth/login.go:17` — Auth bypass: `user == "admin"` short-circuits cred check — delete the branch, route admin through `checkCreds`. [confidence: 99]
- `internal/auth/login.go:13` — Hardcoded API secret committed to source — move to env var, rotate the leaked key, add to `.gitignore`/secret scanner. [confidence: 97]
- `internal/auth/login.go:23` — Password logged in plaintext — remove `pass=%s` from log line; never log credentials. [confidence: 98]

## Compliance Violations
AGENTS.md / CLAUDE.md rule violations.
- `internal/auth/login.go:23` — Logs PII fields (user, pass) — project rule forbids PII in logs; drop the log line or redact. [confidence: 95]

## Design Problems [confidence 80-94]
Wrong abstractions, unnecessary complexity, over-engineering.
- `internal/auth/login.go:22` — Error from `db.Query` discarded via `_` — surface the error; a failing DB call MUST return 500, not silently fall through. [confidence: 90]

## Security & PII [confidence 80-94]
Trust boundaries, auth gaps, data exposure, logging risks.
- `internal/auth/login.go:15` — Credentials passed via URL query string — switch to POST body; query strings land in access logs and browser history. [confidence: 88]

## Maintainability Risks

## Simplification Opportunities

## Nitpicks (Yes, They Matter) [confidence 80-94]
- `internal/auth/login.go:18` — Magic number `200` — use `http.StatusOK`. [confidence: 82]

## What I Would Do Instead
Delete the admin bypass. Parameterize the query. Move the key to env. Drop the PII log line. POST instead of GET for credentials. This change as written is a liability.

## Overall Assessment
This diff introduces four critical security defects and a compliance violation in a single function. Do not ship. REQUEST CHANGES

Review stats: 7 findings from 5 agents, 7 passed confidence filter (≥80), 0 discarded.

Action Items: 4 BLOCKERs, 1 COMPLIANCE, 1 DESIGN, 1 NITPICK — commit blocked

## Action Items
- [ ] BLOCKER `internal/auth/login.go:22` — SQL injection via string concat — use parameterized query [confidence: 98]
- [ ] BLOCKER `internal/auth/login.go:17` — admin auth bypass — delete branch, route through checkCreds [confidence: 99]
- [ ] BLOCKER `internal/auth/login.go:13` — hardcoded API secret — move to env, rotate key [confidence: 97]
- [ ] BLOCKER `internal/auth/login.go:23` — password logged plaintext — remove pass=%s [confidence: 98]
- [ ] COMPLIANCE `internal/auth/login.go:23` — PII in logs — drop or redact log line [confidence: 95]
- [ ] DESIGN `internal/auth/login.go:22` — discarded db.Query error — surface via 500 [confidence: 90]
- [ ] NITPICK `internal/auth/login.go:18` — magic 200 — use http.StatusOK [confidence: 82]
